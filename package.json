{
  "name": "scrollworks-responder",
  "version": "1.0.0",
  "description": "ScrollWorks' own implementation of a JS library that responds to changes in viewport.",
  "main": "responder.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ScrollWorks <ruben@scroll.works> (http://scroll.works)",
  "license": "ISC"
}
